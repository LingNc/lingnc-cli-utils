1. 增加新的选项 -pa 将电脑端存档打包为归档格式，-rp 将归档格式还原到电脑端（默认输入第一个参数为归档位置，电脑使用默认位置，如需指定电脑存档位置应 -r <归档位置> -p <PC存档位置>），还有 -ma 将移动端打包为归档格式（通过ADB即同-a默认行为），-rm 将归档还原为移动端上（通过ADB即同-r默认行为），此处  -ma/-rm 只是兼容选项-r/-a默认行为是操作手机。
2. 增加ADB可以选择设备。
3. 修复电脑同步到手机选项 -pm 会将电脑的setting配置同步过去导致手机setting失效并重置。